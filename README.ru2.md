# Enterprise RAG Challenge с годовыми отчётами

ВНИМАНИЕ ПЕРЕД КЛОНИРОВАНИЕМ: Обратите внимание, что этот репозиторий занимает около 1,4 ГБ (из-за больших PDF-файлов).

## Раунд 1 — сентябрь 2024
Вся информация по первому раунду находится в папке [round1](round1). Обязательно посмотрите `round1/README.md`.

## Раунд 2 — февраль 2025
👉[Целевую страницу с видео результатов и докладом Стефана Гиллича](https://www.timetoact-group.at/details/enterprise-rag-challenge)👈

![image](round2/leaderboard.png)
Интерактивную таблицу лидеров с детальными результатами можно найти [здесь](https://abdullin.com/erc#r2).

Данные для тестового прогона раунда 2 находятся в папке [round2/samples](round2/samples).

## Объяснение

Мы обсуждали различные подходы к созданию AI-ассистентов для компаний. Большая часть обсуждений касалась эффективности и точности разных подходов: RAG-системы с векторными базами данных и Domain-Driven AI Assistants (с Knowledge Maps).

В долгосрочной перспективе технологии не так важны — это детали реализации. Главное — обеспечивает ли система на базе LLM точные ответы или она галлюцинирует.

Мы можем это измерить! Идея началась как дружеское соревнование между несколькими командами. Любой желающий может подключить свою RAG-систему на выбор (или построить её с нуля). Система должна уметь загружать набор публичных годовых отчётов и отвечать на вопросы.

Все вопросы основаны на задачах поиска информации: «Сколько сотрудников у компании X?», «Какая компания имеет большую ликвидность?», «Инвестирует ли компания X в зелёные облигации?»

Кстати, этот тест отражает реальную бизнес-задачу. Во-первых, здесь можно измерить и сравнить точность и частоту галлюцинаций подходов. Во-вторых, QA по большому корпусу данных хорошо подходит для задач вроде генерации лидов или внутренней поддержки.

Участники могут использовать любые технологии: локально, в облаке или сторонние сервисы. Единственные требования:

- Вы можете загрузить тестовый набор публичных годовых отчётов.
- Вы можете загрузить список вопросов в систему и получить список ответов.
- Между шагами 1 и 2 будет задержка в несколько часов, чтобы системы могли проиндексировать данные (например, посчитать embedding-векторы или заполнить карты знаний).
- Нет необходимости делиться исходным кодом вашей системы.

Если хотите участвовать — ниже ссылка на набор примеров PDF для разработки. Тестовый набор будет другим.

## Технические подробности

Мы старались сделать соревнование открытым и справедливым.

Все участники находятся в равных условиях:

1. Мы делимся списком всех годовых отчётов (7496 файлов, ~46 ГБ) вместе с названиями компаний и SHA1-хешами файлов в `dataset.csv`. Эти годовые отчёты являются публичной информацией. **Мы НЕ выкладываем все 46 ГБ сразу, но при желании вы можете найти файлы в интернете.**
2. Мы публикуем код, который генерирует следующий непредсказуемый случайный seed для соревнования. Он использует публичный blockchain API. Смотрите `gen_seed.py`.
3. Мы публикуем генератор вопросов, который случайным образом выбирает подмножество файлов для соревнования и генерирует вопросы. Смотрите `main.py`.
4. Генератор вопросов использует детерминированный RNG, который будет одинаков для всех.

Любой может запустить этот код в момент проведения соревнования. Все гарантированно получат один и тот же список файлов и вопросов одновременно. Никто не должен знать файлы и вопросы заранее.

Как только будет список PDF-файлов, TimeToAct упакует их в zip и опубликует. Каждый сможет проверить SHA1-хеши файлов, чтобы убедиться, что файлы не были изменены.

## Тестовый прогон

Для верификации мы провели тестовый прогон с 20 файлами и 40 вопросами.

Список файлов и вопросов находится в папке [samples](round2/samples). Пояснение процесса ниже.

Сначала мы дождались следующего случайного seed:
```bash
python3 gen_seed.py
# Current block: 855626 at 2024-08-06 08:38:28. Waiting for new block...
# ..............New block found! 855627 at 2024-08-06 08:52:02
# Deterministic seed: 1836201229
```
(обратите внимание, что это использовало старый генератор случайных чисел из `round1/gen_seed.py`)

Затем мы выбрали 20 файлов из `dataset.csv` с этим seed:
```bash
python3 main.py step1 --seed=1836201229 --count=20            ~/tat/enterprise-rag
# 053b7cb83115789346e2a9efc7e2e640851653ff Global Medical REIT Inc.
# 3696c1b29566acc1eafc704ee5737fb3ae6f3d1d Zegona Communications plc
# 40b5cfe0d7bbf59e186492bfbe1b5002d44af332 Calyxt, Inc.
# 4b525836a5d7cb75489f6d93a3b1cf2b8f039bf2 TD SYNNEX
# 58a5f9f5c83159e63602b0b1dd27c27fb945c0e9 Eurocell PLC
# 608c5097dfc6e83505fd2259ad862dcec11a3f96 Sandwell Aquatics Centre
# 6b79f1c1de9d0e39a4576dcd4585849b9465b402 Mercurity Fintech Holding Inc.
# 71b04e0248ecf758990a0ab77bd69344be63bcf4 Motus GI Holdings, Inc.
# 99be213e4e689294ebae809bfa6a1b5024076286 Limbach Holdings, Inc.
# 9ae3bb21564a5098c4b4d6450655c22eff85deae Strike Energy Limited
# 9e703e719d94af786af5511c823ff86e9f04c070 Platform Technology
# 9ff4e041732c9841d5423e6ea0bbd6a0320df9ff VENUS METALS CORPORATION LIMITED
# bd5041c3e6909d92a7a88e4fb10dd8651df33228 NICE
# d734bac4a4815e616d84083ad4d3844655321215 Nykredit
# d81bbc64a4160b9946fea7a895f80e6201f52f27 Air Products
# dd78f748262b8ffa62de6484143ff55b38af24c7 Accuray Incorporated
# dfb1e552b18e116105d9125d9becafa443950e97 Kooth Plc
# e51b7204b91cbe7709bd3218e7d2d0c2b8dbb438 Ethernity Networks Ltd
# ea0757d27fa67cd347d9f046b939a911f5c9a08d Canadian Banc Corp.
# faf8d7d79152d61279eda1cfb58b8236ce2f82fa EMT
```

Эти файлы загружены в `round2/samples`. Вы можете проверить их хеш с помощью кода:

```py
import hashlib
from pathlib import PosixPath

def sha1_hash(file_path: PosixPath) -> str:
    sha1 = hashlib.sha1()
    with file_path.open('rb') as f:
        while chunk := f.read(8192):
            sha1.update(chunk)
    return sha1.hexdigest()
```

Далее мы дождались следующего случайного seed:

```bash
python3 gen_seed.py
# New block found! 855628 at 2024-08-06 08:58:35
# Deterministic seed: 3031428637
```
(опять же — старый генератор из `round1`)

Затем сгенерировали вопросы командой:

```bash
python3 main.py step2 --seed=3031428637 --count=40
```

Он сгенерировал вопросы, например (полный список в `round2/samples/questions.json`):

1. number: How much did "Accuray Incorporated" spend on risk management in Q2 2022?
2. name: Who is the CEO in the company "Zegona Communications plc"?
3. boolean: Did "Global Medical REIT Inc." have a greater Debt-to-Equity ratio than "Zegona Communications plc" in Q2 2021?
4. number: How many stores did "Accuray Incorporated" have in the end of fiscal year 2021?
5. number: How much did "Sandwell Aquatics Centre" spend on R&D in Q2 2023?
6. name: Who is the CFO in the company "EMT"?
7. boolean: Did "Calyxt, Inc." have a greater Return on Assets (ROA) than "Global Medical REIT Inc." in Q2 2023?

Итоговые seed для `round2`:
- создание выборки: 77040339 (проверяется через https://api.drand.sh/public/4840674)
- генерация вопросов: 96461695 (проверяется через https://api.drand.sh/public/4840954)

## Схема

### Схема генератора

Обратите внимание на схему для каждого вопроса. Она соответствует следующему pydantic-схеме:

```py
from pydantic import BaseModel
from typing import Literal

class Question(BaseModel):
    text: str
    kind: Literal["number", "name", "boolean", "names"]
```


### Схема ответа (Submission Schema)

Для отправки результатов рекомендуем использовать следующую схему:

```py
from pydantic import BaseModel, Field
from typing import Optional, List, Union, Literal

class SourceReference(BaseModel):
    pdf_sha1: str = Field(..., description="SHA1 hash of the PDF file")
    page_index: int = Field(..., description="Zero-based physical page number in the PDF file")

class Answer(BaseModel):
    question_text: Optional[str] = Field(None, description="Text of the question")
    kind: Optional[Literal["number", "name", "boolean", "names"]] = Field(None, description="Kind of the question")
    value: Union[float, str, bool, List[str], Literal["N/A"]] = Field(..., description="Answer to the question, according to the question schema")
    references: List[SourceReference] = Field([], description="References to the source material in the PDF file")

class AnswerSubmission(BaseModel):
    team_email: str = Field(..., description="Email that your team used to register for the challenge")
    submission_name: str = Field(..., description="Unique name of the submission (e.g. experiment name)")
    answers: List[Answer] = Field(..., description="List of answers to the questions")
```

Правила форматов ответов:

* number — ожидается только число. Без разделителей и суффиксов. Правильно: `122333`. Неправильно: `122k`, `122 233`, `122,333`.
* name — ожидается только имя (персона/должность/лицо).
* names — список имён.
* boolean — только `yes`/`no` или `true`/`false` (регистр не важен).

Важно! Во всех схемах допускается значение `N/A` или `n/a`, которое означает «не применимо» или «недостаточно информации даже для человека».

Добавление необязательного поля `question_text` в `Answer` помогает API сверять порядок вопросов для корректного подсчёта баллов.

## Отправка (Submission)

Отправка выполнялась через предоставленный API и веб-интерфейс (исходники: https://github.com/trustbit/enterprise-rag-challenge-ui) с проверяемой TSP-подписью (https://github.com/pyauth/tsp-client).

Все отправленные анонимизированные ответы можно найти в папке `round2/submissions`.

## Часто задаваемые вопросы

В: Поделитесь ли вы всеми 46 ГБ годовых отчётов?

О: Нет, мы публикуем только подмножество для соревнования. Эти отчёты являются публичными; при желании вы можете собрать их самостоятельно.

В: Почему вопросы содержат имена компаний, которых нет в PDF?

О: Цель — обнаружить галлюцинации. Если данные о компании отсутствуют в наборе PDF, RAG-система должна ответить `N/A`.

В: Почему некоторые вопросы кажутся бессмысленными?

О: Вы увидите вопросы, которые на первый взгляд в порядке, но для конкретной компании бессмысленны. Например:

* number: How many stores did "Strike Energy Limited" have in the end of fiscal year 2021?

Как в реальном мире — не на все вопросы есть релевантная информация. Если у компании нет магазинов, ответ — `N/A`.

Плохая RAG-система даст правдоподобную, но неверную цифру. Хорошая — верно вернёт `N/A`.

В: Как проверяются правильные ответы?

О: TIMETOACT собирает все ответы, затем вручную проводит ревью и формирует эталонные ответы. Они будут опубликованы вместе с оценками.

В: Хочу провести похожий челлендж с таблицами, другим языком или индустрией. Как?

О: Этот репозиторий — Apache-2.0, можете форкать и использовать без ограничений. Подход:

1. Соберите собственный набор исходных файлов.
2. Настройте генератор вопросов под вашу предметную область.
3. Следуйте общему процессу.

Мы только просим соблюдать условия лицензии Apache 2.0.
